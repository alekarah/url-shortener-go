# URL Shortener (Go)

[![CI](https://github.com/alekarah/url-shortener-go/actions/workflows/ci.yml/badge.svg)](https://github.com/alekarah/url-shortener-go/actions/workflows/ci.yml)

Сервис для сокращения URL с аналитикой кликов, написанный на Go.

## Возможности

- 🔗 Сокращение длинных URL в короткие коды
- 🎯 Поддержка кастомных коротких кодов
- 📊 Аналитика кликов с детальной статистикой
- 🌐 Веб-интерфейс для создания и просмотра ссылок
- 📋 Страница со списком всех созданных ссылок
- ⚡ Кеширование в Redis для быстрого доступа
- 🐳 Docker контейнеры для PostgreSQL и Redis
- 🔄 SQL миграции для версионирования схемы БД
- 🏗️ Clean Architecture с разделением на слои

## Технологии

- **Go 1.22** - язык программирования
- **Chi v5** - HTTP роутер и middleware
- **PostgreSQL 16** - основная база данных
- **Redis 7** - кеширование
- **golang-migrate** - миграции БД
- **Docker Compose** - контейнеризация

## Архитектура

```
url-shortener-go/
├── cmd/server/          # Точка входа приложения
├── internal/
│   ├── config/          # Конфигурация из .env
│   ├── database/        # Подключения к PostgreSQL и Redis
│   ├── handlers/        # HTTP обработчики (контроллеры)
│   ├── middleware/      # Middleware (логирование)
│   ├── models/          # Структуры данных
│   ├── repository/      # Слой работы с БД
│   └── service/         # Бизнес-логика
├── pkg/shortener/       # Генератор коротких кодов (Base62)
├── static/              # Фронтенд (HTML, CSS, JS)
│   ├── css/            # Стили
│   ├── js/             # JavaScript
│   ├── index.html      # Главная страница
│   └── links.html      # Страница со списком ссылок
├── migrations/          # SQL миграции
└── docker-compose.yml   # Docker конфигурация
```

## Быстрый старт

### Предварительные требования

- Go 1.22+
- Docker Desktop
- golang-migrate CLI

Установка golang-migrate:
```bash
go install -tags 'postgres' github.com/golang-migrate/migrate/v4/cmd/migrate@latest
```

### Установка и запуск

1. Клонируй репозиторий:
```bash
git clone <your-repo-url>
cd url-shortener-go
```

2. Создай `.env` файл из примера:
```bash
cp .env.example .env
```

3. Запусти Docker контейнеры и примени миграции:
```bash
docker-compose up -d
migrate -path ./migrations -database "postgres://postgres:postgres@localhost:5433/url_shortener?sslmode=disable" up
```

4. Запусти сервер:
```bash
go run cmd/server/main.go
```

Сервер будет доступен на `http://localhost:8080`

### Веб-интерфейс

- **Главная страница** - `http://localhost:8080` - создание коротких ссылок
- **Список всех ссылок** - `http://localhost:8080/links` - просмотр всех созданных ссылок с пагинацией

### Остановка

```bash
docker-compose down
```

## Веб-интерфейс

### Главная страница (/)

- Форма для создания коротких ссылок
- Поддержка кастомных кодов
- Мгновенное отображение результата с кнопкой копирования
- Статистика созданной ссылки (ID, код, дата, клики)

### Страница всех ссылок (/links)

- Таблица со всеми созданными ссылками
- Пагинация (20 ссылок на страницу)
- Отображение: короткий код, оригинальный URL, количество кликов, дата создания
- Кликабельные короткие ссылки
- Статистика по текущей странице

## API Endpoints

### Получение списка всех URL

**GET** `/api/v1/urls`

Query параметры:
- `limit` - количество записей (по умолчанию 50, макс 100)
- `offset` - смещение для пагинации (по умолчанию 0)

Пример:
```bash
curl "http://localhost:8080/api/v1/urls?limit=20&offset=0"
```

Ответ:
```json
[
  {
    "id": 1,
    "short_code": "abc123",
    "short_url": "http://localhost:8080/abc123",
    "original_url": "https://example.com",
    "created_at": "2025-12-15T10:00:00Z",
    "clicks_count": 42
  }
]
```

### Создание короткой ссылки

**POST** `/api/v1/urls`

Тело запроса:
```json
{
  "original_url": "https://www.example.com",
  "custom_code": "mycode",  // опционально
  "expires_at": "2025-12-31T23:59:59Z"  // опционально
}
```

Ответ:
```json
{
  "id": 1,
  "short_code": "mycode",
  "short_url": "http://localhost:8080/mycode",
  "original_url": "https://www.example.com",
  "created_at": "2025-12-15T10:00:00Z",
  "expires_at": "2025-12-31T23:59:59Z",
  "clicks_count": 0
}
```

### Получение информации о ссылке

**GET** `/api/v1/urls/{id}`

Ответ:
```json
{
  "id": 1,
  "short_code": "mycode",
  "short_url": "http://localhost:8080/mycode",
  "original_url": "https://www.example.com",
  "created_at": "2025-12-15T10:00:00Z",
  "clicks_count": 42
}
```

### Удаление ссылки

**DELETE** `/api/v1/urls/{id}`

### Получение статистики

**GET** `/api/v1/urls/{id}/stats`

Ответ:
```json
{
  "total_clicks": 42,
  "unique_ips": 15,
  "clicks_by_date": [
    {
      "date": "2025-12-15T00:00:00Z",
      "count": 10
    }
  ],
  "clicks_by_country": [],
  "recent_clicks": [
    {
      "id": 1,
      "url_id": 1,
      "clicked_at": "2025-12-15T10:30:00Z",
      "ip_address": "127.0.0.1",
      "user_agent": "Mozilla/5.0...",
      "referer": ""
    }
  ]
}
```

### Редирект

**GET** `/{shortCode}`

Перенаправляет на оригинальный URL и записывает аналитику.

## Примеры использования

### Веб-интерфейс (рекомендуется)

1. Открой `http://localhost:8080` в браузере
2. Введи длинный URL (например, `https://www.youtube.com/watch?v=dQw4w9WgXcQ`)
3. Опционально укажи кастомный код (например, `yt`)
4. Нажми "Сократить ссылку"
5. Скопируй созданную короткую ссылку
6. Перейди на `/links` чтобы увидеть все созданные ссылки

### API (curl)

#### Создание ссылки с автоматическим кодом

```bash
curl -X POST http://localhost:8080/api/v1/urls \
  -H "Content-Type: application/json" \
  -d '{"original_url": "https://www.google.com"}'
```

#### Создание ссылки с кастомным кодом

```bash
curl -X POST http://localhost:8080/api/v1/urls \
  -H "Content-Type: application/json" \
  -d '{"original_url": "https://github.com", "custom_code": "github"}'
```

#### Получение списка всех ссылок

```bash
curl "http://localhost:8080/api/v1/urls?limit=10&offset=0"
```

#### Получение статистики

```bash
curl http://localhost:8080/api/v1/urls/1/stats
```

#### Открытие короткой ссылки

Просто открой в браузере:
```
http://localhost:8080/github
```

## Тестирование

```bash
# Запустить тесты
go test ./... -v

# С покрытием
go test ./... -cover

# Проверка перед push (тесты + форматирование + линтер)
./check-code.sh
```

**Покрытие:**
- `pkg/shortener` - 72.5% (генератор Base62, валидация)
- `internal/service` - 19.2% (бизнес-логика)
- `internal/handlers` - 18.6% (HTTP endpoints)

**33 теста** включают:
- Unit тесты: генерация кодов, уникальность, валидация
- Service тесты: создание ссылок, дубликаты, пагинация
- Integration тесты: POST/GET/DELETE endpoints, обработка ошибок

## База данных

### Схема

**Таблица urls:**
- `id` - уникальный идентификатор
- `short_code` - короткий код (уникальный)
- `original_url` - оригинальный URL
- `created_at` - время создания
- `expires_at` - время истечения (опционально)
- `user_id` - ID пользователя (опционально)
- `clicks_count` - счетчик кликов
- `last_clicked_at` - время последнего клика

**Таблица analytics:**
- `id` - уникальный идентификатор
- `url_id` - ссылка на urls (CASCADE)
- `clicked_at` - время клика
- `ip_address` - IP адрес
- `user_agent` - User Agent браузера
- `referer` - Referer страница
- `country`, `city` - геолокация (опционально)

### Миграции

Создание новой миграции:
```bash
migrate create -ext sql -dir migrations -seq migration_name
```

Применение миграций:
```bash
migrate -path ./migrations -database "postgres://postgres:postgres@localhost:5433/url_shortener?sslmode=disable" up
```

Откат последней миграции:
```bash
migrate -path ./migrations -database "postgres://postgres:postgres@localhost:5433/url_shortener?sslmode=disable" down 1
```

## Конфигурация

Все настройки хранятся в `.env` файле:

```env
# PostgreSQL
POSTGRES_HOST=localhost
POSTGRES_PORT=5433
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=url_shortener

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Server
SERVER_PORT=8080
BASE_URL=http://localhost:8080

# Cache
CACHE_TTL=3600
```

## Особенности реализации

### Генерация коротких кодов

Используется Base62 кодирование (0-9, A-Z, a-z) для генерации коротких кодов длиной 7 символов. Это дает 62^7 = 3.5 триллиона возможных комбинаций.

### Кеширование

Redis используется для кеширования маппинга `short_code → original_url` с TTL 3600 секунд (1 час). Это снижает нагрузку на PostgreSQL.

### Аналитика

Запись аналитики происходит в фоновой горутине, чтобы не замедлять редирект. Используется `context.Background()` для избежания отмены контекста после завершения HTTP запроса.

### Редирект

Используется HTTP 302 (Found) вместо 301 (Moved Permanently), чтобы браузеры не кешировали редирект. Это гарантирует, что каждый клик будет зарегистрирован.

## Возможные улучшения

### Backend
- [ ] Аутентификация и авторизация пользователей (JWT)
- [ ] Rate limiting для предотвращения злоупотреблений
- [ ] Интеграция с GeoIP для определения страны/города кликов
- [ ] Webhook уведомления о кликах
- [ ] A/B тестирование с несколькими destination URL
- [ ] Bulk API для массового создания ссылок
- [ ] Экспорт статистики в CSV/JSON

### Frontend
- [ ] QR коды для коротких ссылок
- [ ] Графики статистики (Chart.js)
- [ ] Поиск и фильтрация ссылок на странице /links
- [ ] Сортировка по дате/кликам
- [ ] Темная тема
- [ ] Возможность удаления ссылок (после добавления авторизации)
- [ ] Редактирование ссылок (изменение original_url)

### DevOps
- [x] CI/CD pipeline (GitHub Actions)
- [ ] Docker production образ
- [ ] Мониторинг (Prometheus + Grafana)
- [ ] Логирование в файлы/Loki

## Лицензия

MIT

## Автор

alekarah